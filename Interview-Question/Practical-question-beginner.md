# **Practical based question with answer (Beginner level)**

1. **Reverse a string in python**

   ```
   text="bella"
   rev_text=text[::-1]
   print(rev_text)
   ```

2. **Factorial of a number**

   ```
   def factorial(n):
       if n==0;
          return 1
       else:
          return n * factorial(n-1)
   print(factorial(5))
   ```

3. **Check a number is prime or not**

   ```
   def isprime(n):
       if num<=1:
          return false
       for i in range(2,int(num**0.5)+1):
           if num % i == 0:
              return false
       return true
   print(isprime(11))
   ```

